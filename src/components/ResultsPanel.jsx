/**
 * ResultsPanel — streaming results display.
 * Red flags render the moment they are detected (Tier 1), yellow as Tier 2
 * completes, green passes collapse to one line. Implemented in Step 4.
 */
export default function ResultsPanel() {
  return null;
}
