/**
 * ErrorMessage — standardized error display.
 * Every error states exactly what happened and what to do next.
 * Implemented in Step 7.
 */
export default function ErrorMessage() {
  return null;
}
