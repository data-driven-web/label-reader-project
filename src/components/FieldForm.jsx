/**
 * FieldForm — manual application field entry.
 * Pre-populates from extraction results; auto-filled fields get a yellow
 * highlight so the agent knows what the AI read. Implemented in Step 5.
 */
export default function FieldForm() {
  return null;
}
